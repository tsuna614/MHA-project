import 'package:flutter/material.dart';

class Delete_Room_Screen extends StatelessWidget {
  const Delete_Room_Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Delete Room'),
      ),
    );
  }
}
