import 'package:flutter/material.dart';

class Create_Room_Screen extends StatelessWidget {
  const Create_Room_Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create Room'),
      ),
    );
  }
}
