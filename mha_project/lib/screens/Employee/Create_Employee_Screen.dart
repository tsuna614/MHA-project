import 'package:flutter/material.dart';

class Create_Employee_Screen extends StatelessWidget {
  const Create_Employee_Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create Employee'),
      ),
    );
  }
}
