import 'package:flutter/material.dart';

class Delete_Employee_Screen extends StatelessWidget {
  const Delete_Employee_Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Delete Employee'),
      ),
    );
  }
}
