import 'package:flutter/material.dart';

class Delete_Services_Screen extends StatelessWidget {
  const Delete_Services_Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Delete Services'),
      ),
    );
  }
}
