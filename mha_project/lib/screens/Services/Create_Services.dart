import 'package:flutter/material.dart';

class Create_Services_Screen extends StatelessWidget {
  const Create_Services_Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create Services'),
      ),
    );
  }
}
