import 'package:flutter/material.dart';

class View_Services_Screen extends StatelessWidget {
  const View_Services_Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('View Services'),
      ),
    );
  }
}
